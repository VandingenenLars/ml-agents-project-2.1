# Group 10 Project 2.1 - AI Machine Learning

## Overview
This project focuses on the collection and processing of data generated by AI agents trained using the Unity ML-Agents Toolkit, creating a structured dataset suitable for machine learning. The processed data serves as input to a predictive model which is designed to answer specific research questions.

This repository provides all components necessary for a reproducible workflow, including scripts for data collection, preprocessing, feature extraction, training and evaluation the predictive model and visualizating the results. this forms a complete pipeline from raw agent trainining to a trained predictor model.

## Virtual Enviroment Setup and Installation
1. Install Miniconda: https://docs.conda.io/en/latest/miniconda.html
2. Clone the repo:
   git clone https://github.com/VandingenenLars/ml-agents-project-2.1.git
3. Create and activate the environment:
   conda env create -f environment.yml
   conda activate ml-agents
4. Run training:
   mlagents-learn config/ppo.yaml --run-id=MyRun
5. Open Unity and press Play.
