# Group 10 Project 2.1 - AI Machine Learning

## Overview
This project focuses on the collection and processing of data generated by AI agents trained using the Unity ML-Agents Toolkit, creating a structured dataset suitable for machine learning. The processed data serves as input to a predictive model which is designed to answer specific research questions.

This repository provides all components necessary for a reproducible workflow, including scripts for data collection, preprocessing, feature extraction, training and evaluation the predictive model and visualizating the results. this forms a complete pipeline from raw agent trainining to a trained predictor model.

## Deliverable's Project Structure
```
├── deliverables 
│ ├── data 
│ │ ├── features # Feature matrices and summaries 
│ │ ├── processed # Preprocessed datasets for training/testing
│ │ └── raw # Raw datasets and metadata
│ ├── model
│ │ ├── evaluator # Scripts to evaluate models and visualize metrics
│ │ └── predictor # Scripts to run inference or prediction
│ ├── training
│ │ ├── configs # Training configuration files
│ │ │ ├── ppo
│ │ │ └── sac
│ │ ├── TrainingOrchestrator.py # Main script to orchestrate training
│ │ ├── logs # Logs generated during training
│ │ └── results # Saved models and training results
│ ├── utils
│ │ ├── data_processor.py # Responsible for processing the raw data
│ │ ├── feature_extractor.py # Transform processed data into feature sets
│ │ └── training_helpers.py # Training utility functions
│ └── environment.yml # Conda environment specification
```
## Virtual Enviroment Setup and Installation

This project uses **Conda** for environment management. To set it up:

1. **Install Conda** (if not already installed):   
   - [Miniconda](https://docs.conda.io/en/latest/miniconda.html) or [Anaconda](https://www.anaconda.com/).  
_(Miniconda is lighter and sufficient for our tasks, therefore preferred)_  
2. **Create the environment** from the provided **environment.yml** file:
```bash
conda env create -f deliverables/environment.yml
```
3. **Activating** the environment:
```bash
conda activate mlagents
```
4. **Verify installation**:
```bash
conda list
```
_If `mlagents' environment is present then setup was sucessful._

## Usage Example
1. **Activate** the environment
```bash
conda activate mlagents
```

2. **Run** `TrainingOrchestrator.py`
> While in root directory:
```bash
python deliverables/training/TrainingOrchestrator.py --config deliverables/training/configs/{algo}/{config.yml}
```
